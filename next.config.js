/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.pexels.com",
      "onedrive.live.com",
      "bnz05pap001files.storage.live.com",
    ],
  },
};

module.exports = nextConfig;
